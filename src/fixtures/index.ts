/**
 * fixtures/index.ts — Barrel export for test fixtures.
 * Specs can `import { test, expect } from '@fixtures/index'`.
 */
export { test, expect } from './api.fixtures.js';
export type { ApiFixtures } from './api.fixtures.js';
